"use client";

import { useEffect, useMemo, useState } from "react";
import { useRouter } from "next/navigation";
import { ArrowRight, Check, CheckCircle2, CircleHelp, MapPin, PackageCheck, Ruler, Sparkles, Truck, Wrench } from "lucide-react";
import Button from "@/components/ui/Button";
import { useCustomerAuth } from "@/context/CustomerAuthContext";
import { formatPrice } from "@/lib/utils";

type ProjectProfile = {
  projectType: string;
  useCase: string;
  loadRequirement: "light" | "medium" | "heavy";
  surfaceType: "soil" | "concrete" | "existing-gravel" | "mixed";
  drainagePriority: "low" | "medium" | "high";
  style: string;
  colorPreference: string;
  maintenance: "low" | "medium" | "high";
  indoorOutdoor: "indoor" | "outdoor" | "both";
  budget: "economy" | "standard" | "premium";
};

type Recommendation = { id: string; name: string; description: string; image: string; unit: string; price: number; coveragePerUnit: number; wastagePercent: number; matchReasons?: string[]; estimate: { recommendedQuantity: number; materialTotal: number; baseQuantity: number } };
type Service = { id: string; name: string; description: string; pricingModel: "flat" | "per_sqm"; price: number; unit: string; total?: number };
type DeliveryZone = { id: string; name: string; fee: number; min_order_for_free?: number; estimated_days?: string };
type QuestionKey = keyof ProjectProfile;

const INITIAL_PROFILE: ProjectProfile = { projectType: "", useCase: "", loadRequirement: "medium", surfaceType: "soil", drainagePriority: "medium", style: "", colorPreference: "any", maintenance: "medium", indoorOutdoor: "outdoor", budget: "standard" };
const QUESTIONS: { key: QuestionKey; title: string; help: string; options: { value: string; label: string; description: string }[] }[] = [
  { key: "projectType", title: "What are you building?", help: "This helps us distinguish between decorative, access, and structural needs.", options: [{ value: "landscaping", label: "Landscape feature", description: "Garden beds, borders, accents" }, { value: "garden", label: "Garden area", description: "Planting beds and outdoor spaces" }, { value: "pathway", label: "Walkway or patio", description: "Pedestrian paths and sitting areas" }, { value: "driveway", label: "Driveway or parking", description: "Vehicle access and parking" }, { value: "drainage", label: "Drainage or erosion", description: "Runoff, slopes, and water control" }, { value: "construction", label: "Construction base", description: "A stable base or fill application" }] },
  { key: "useCase", title: "What will the material do?", help: "Choose the main job so we can prioritize the right performance characteristics.", options: [{ value: "decorative", label: "Add a decorative finish", description: "Color, texture, and visual appeal" }, { value: "planting-bed", label: "Cover a planting bed", description: "A neat, low-maintenance garden surface" }, { value: "walkway-base", label: "Support foot traffic", description: "A practical walking surface or base" }, { value: "driveway-base", label: "Support vehicles", description: "A stronger base for cars or access" }, { value: "drainage", label: "Improve drainage", description: "Help water move through the area" }, { value: "erosion-control", label: "Control erosion", description: "Protect soil and exposed slopes" }] },
  { key: "loadRequirement", title: "How much traffic or load will it receive?", help: "A driveway needs a different material profile than a decorative garden border.", options: [{ value: "light", label: "Light use", description: "Decorative areas or occasional walking" }, { value: "medium", label: "Regular foot traffic", description: "Paths, patios, and everyday use" }, { value: "heavy", label: "Vehicles or heavy loads", description: "Driveways, parking, or equipment" }] },
  { key: "surfaceType", title: "What surface are you starting from?", help: "The existing base affects preparation and the material system we recommend.", options: [{ value: "soil", label: "Soil or garden bed", description: "Natural ground or planting soil" }, { value: "concrete", label: "Concrete or slab", description: "An existing hard surface" }, { value: "existing-gravel", label: "Existing gravel", description: "A previous aggregate base" }, { value: "mixed", label: "Mixed or unsure", description: "Different surfaces across the project" }] },
  { key: "drainagePriority", title: "How important is drainage?", help: "Tell us whether managing rainwater is a key part of the project.", options: [{ value: "low", label: "Not a major concern", description: "The area already drains well" }, { value: "medium", label: "Some attention needed", description: "We want a practical solution" }, { value: "high", label: "High priority", description: "Water pooling or runoff is a concern" }] },
  { key: "style", title: "Which look are you aiming for?", help: "Style helps us rank materials with a finish and texture that fit the space.", options: [{ value: "natural", label: "Natural", description: "Organic, earthy, and relaxed" }, { value: "modern", label: "Modern", description: "Clean lines and contemporary contrast" }, { value: "rustic", label: "Rustic", description: "Warm, textured, and traditional" }, { value: "tropical", label: "Tropical", description: "Lush, bright, and garden-focused" }, { value: "clean", label: "Clean and simple", description: "Neat, minimal, and easy to maintain" }] },
  { key: "colorPreference", title: "Do you have a color preference?", help: "Choose any if you would rather let the available inventory guide you.", options: [{ value: "any", label: "Open to options", description: "Show the best available matches" }, { value: "neutral", label: "Neutral", description: "Soft gray, beige, or balanced tones" }, { value: "white", label: "Light or white", description: "Bright, clean, and reflective" }, { value: "dark", label: "Dark or charcoal", description: "Strong contrast and modern depth" }, { value: "warm", label: "Warm earth tones", description: "Tan, brown, terracotta, or golden" }, { value: "mixed", label: "Mixed colors", description: "Natural variation and visual texture" }] },
  { key: "maintenance", title: "How much maintenance do you prefer?", help: "This helps balance appearance with long-term upkeep.", options: [{ value: "low", label: "Keep it low", description: "Minimal refreshing and maintenance" }, { value: "medium", label: "Some upkeep is fine", description: "A balance of appearance and effort" }, { value: "high", label: "I enjoy maintaining it", description: "Prioritize detail and visual control" }] },
  { key: "indoorOutdoor", title: "Where will the material be used?", help: "Some materials are better suited to weather exposure or indoor display.", options: [{ value: "outdoor", label: "Outdoor", description: "Garden, yard, path, or driveway" }, { value: "indoor", label: "Indoor", description: "Interior feature or display" }, { value: "both", label: "Indoor and outdoor", description: "I want a flexible material" }] },
  { key: "budget", title: "What is your starting budget?", help: "This filters the ranking without preventing you from seeing other available options.", options: [{ value: "economy", label: "Economy", description: "Prioritize practical value" }, { value: "standard", label: "Standard", description: "Balance cost and finish" }, { value: "premium", label: "Premium", description: "Prioritize finish and presentation" }] },
];

const DISCLAIMER = "This quotation is an estimate based on the information provided. Final quantity, delivery charges, material requirements, and pricing may be confirmed by our team after reviewing your project.";

export default function EstimatorPage() {
  const router = useRouter();
  const { user, isAuthenticated } = useCustomerAuth();
  const [length, setLength] = useState(""); const [width, setWidth] = useState(""); const [area, setArea] = useState("");
  const [deliveryCity, setDeliveryCity] = useState(""); const [deliveryZoneId, setDeliveryZoneId] = useState(""); const [deliveryZones, setDeliveryZones] = useState<DeliveryZone[]>([]); const [timeline, setTimeline] = useState(""); const [notes, setNotes] = useState("");
  const [profile, setProfile] = useState<ProjectProfile>(INITIAL_PROFILE); const [questionnaireOpen, setQuestionnaireOpen] = useState(false); const [questionIndex, setQuestionIndex] = useState(0);
  const [recommendations, setRecommendations] = useState<Recommendation[]>([]); const [services, setServices] = useState<Service[]>([]); const [selectedProductId, setSelectedProductId] = useState(""); const [selectedServiceIds, setSelectedServiceIds] = useState<string[]>([]); const [estimate, setEstimate] = useState<any>(null);
  const [loading, setLoading] = useState(false); const [submitting, setSubmitting] = useState(false); const [message, setMessage] = useState(""); const [error, setError] = useState("");

  useEffect(() => { fetch("/api/delivery").then((response) => response.json()).then((data) => setDeliveryZones(data.zones || [])).catch(() => {}); const query = new URLSearchParams(window.location.search); if (query.get("area")) setArea(query.get("area") || ""); if (query.get("material")) setSelectedProductId(query.get("material") || ""); }, []);
  const computedArea = useMemo(() => { const direct = Number(area); if (direct > 0) return direct; const parsedLength = Number(length); const parsedWidth = Number(width); return parsedLength > 0 && parsedWidth > 0 ? parsedLength * parsedWidth : 0; }, [area, length, width]);
  const selectedRecommendation = recommendations.find((item) => item.id === selectedProductId) || recommendations[0];
  const currentTotals = estimate?.totals || { materialTotal: selectedRecommendation?.estimate.materialTotal || 0, deliveryFee: 0, serviceTotal: 0, otherCharges: 0, discount: 0, total: selectedRecommendation?.estimate.materialTotal || 0 };
  const profileComplete = QUESTIONS.every((question) => Boolean(profile[question.key]));
  const activeStep = questionnaireOpen ? 2 : estimate ? 3 : 1;

  const calculate = async (nextProductId = selectedProductId, nextServiceIds = selectedServiceIds, nextProfile = profile) => {
    setError(""); if (!computedArea || computedArea <= 0) { setError("Enter a valid area, or enter both length and width in meters."); return null; } setLoading(true);
    try { const response = await fetch("/api/estimate", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ areaSqm: computedArea, serviceIds: nextServiceIds, selectedProductId: nextProductId, deliveryZoneId, projectProfile: nextProfile }) }); const data = await response.json(); if (!response.ok) throw new Error(data.error || "Unable to calculate estimate"); setRecommendations(data.recommendations || []); setServices(data.services || []); setEstimate(data); if (data.selectedProductId && !nextProductId) setSelectedProductId(data.selectedProductId); return data; } catch (err: any) { setError(err.message || "Unable to calculate estimate"); return null; } finally { setLoading(false); }
  };

  const startQuestionnaire = () => { setError(""); if (!computedArea || computedArea <= 0) { setError("Enter your project area first, then we will ask a few questions to match materials."); return; } setQuestionnaireOpen(true); setQuestionIndex(0); };
  const chooseAnswer = (value: string) => { const question = QUESTIONS[questionIndex]; setProfile((previous) => ({ ...previous, [question.key]: value })); };
  const nextQuestion = async () => { const question = QUESTIONS[questionIndex]; if (!profile[question.key]) { setError("Choose an answer so we can make a useful recommendation."); return; } setError(""); if (questionIndex < QUESTIONS.length - 1) { setQuestionIndex((current) => current + 1); return; } if (!profileComplete) { setError("Please complete the remaining questions."); return; } setQuestionnaireOpen(false); await calculate(selectedProductId, selectedServiceIds, profile); };
  const chooseMaterial = async (id: string) => { setSelectedProductId(id); await calculate(id, selectedServiceIds, profile); };
  const toggleService = async (id: string) => { const next = selectedServiceIds.includes(id) ? selectedServiceIds.filter((item) => item !== id) : [...selectedServiceIds, id]; setSelectedServiceIds(next); await calculate(selectedProductId, next, profile); };

  const requestQuotation = async () => {
    if (!selectedRecommendation || !computedArea) { setError("Complete the project questions and select a material first."); return; }
    if (!isAuthenticated) { sessionStorage.setItem("pendingEstimate", JSON.stringify({ areaSqm: computedArea, selectedProductId, selectedServiceIds, projectProfile: profile, deliveryCity, deliveryZoneId, timeline, notes })); router.push("/login?redirect=/estimator"); return; }
    setSubmitting(true); setError("");
    try { const response = await fetch("/api/quotes", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ items: [{ productId: selectedRecommendation.id, name: selectedRecommendation.name, quantity: selectedRecommendation.estimate.recommendedQuantity, unit: selectedRecommendation.unit, estimatedUnitPrice: selectedRecommendation.price, totalPrice: selectedRecommendation.estimate.materialTotal, coveragePerUnit: selectedRecommendation.coveragePerUnit }], areaSqm: computedArea, selectedMaterialId: selectedRecommendation.id, services: estimate?.services || [], materialTotal: currentTotals.materialTotal, deliveryFee: currentTotals.deliveryFee, serviceTotal: currentTotals.serviceTotal, otherCharges: currentTotals.otherCharges, discount: currentTotals.discount, total: currentTotals.total, notes, projectType: profile.projectType, deliveryCity, timeline, projectLocation: deliveryCity, deliveryZoneId, projectDetails: { projectProfile: profile, useCase: profile.useCase, loadRequirement: profile.loadRequirement, surfaceType: profile.surfaceType, drainagePriority: profile.drainagePriority, style: profile.style, colorPreference: profile.colorPreference, maintenance: profile.maintenance, indoorOutdoor: profile.indoorOutdoor, budget: profile.budget }, customerName: [user?.firstName, user?.lastName].filter(Boolean).join(" "), customerEmail: user?.email || "", customerPhone: user?.phone || "", estimateDisclaimer: DISCLAIMER }) }); const data = await response.json(); if (!response.ok) throw new Error(data.error || "Failed to submit quotation"); setMessage(`Quotation ${data.quoteNumber} submitted successfully. Our team will review it and contact you.`); } catch (err: any) { setError(err.message || "Failed to submit quotation"); } finally { setSubmitting(false); }
  };

  const question = QUESTIONS[questionIndex];
  return <main className="min-h-screen bg-stone-50"><section className="bg-slate-950 text-white"><div className="container-custom py-14 sm:py-20"><div className="max-w-3xl"><div className="inline-flex items-center gap-2 rounded-full border border-emerald-300/20 bg-emerald-300/10 px-3 py-1.5 text-xs font-semibold uppercase tracking-[.18em] text-emerald-200"><Sparkles className="h-4 w-4" /> Guided project estimator</div><h1 className="mt-5 text-4xl font-semibold tracking-[-.04em] sm:text-6xl">Answer a few project questions. Get a smarter material shortlist.</h1><p className="mt-5 max-w-2xl text-base leading-8 text-slate-300 sm:text-lg">We use your area, intended use, traffic, drainage, style, maintenance, and budget to rank materials that are active and available in your inventory.</p></div><div className="mt-10 grid gap-3 sm:grid-cols-3">{[[1, "Project area"], [2, "Project questions"], [3, "Material shortlist"]].map(([number, label]) => <div key={number} className={`flex items-center gap-3 rounded-2xl border px-4 py-3 ${activeStep >= Number(number) ? "border-emerald-300/40 bg-emerald-300/10" : "border-white/10 bg-white/5"}`}><span className={`flex h-8 w-8 items-center justify-center rounded-full text-sm font-bold ${activeStep >= Number(number) ? "bg-emerald-400 text-slate-950" : "bg-white/10 text-slate-300"}`}>{activeStep > Number(number) ? <Check className="h-4 w-4" /> : number}</span><span className="text-sm font-medium text-slate-200">{label}</span></div>)}</div></div></section>
    <section className="container-custom -mt-8 pb-20"><div className="grid gap-6 lg:grid-cols-[.9fr_1.1fr] lg:items-start"><div className="rounded-[2rem] border border-slate-200 bg-white p-6 shadow-xl shadow-slate-900/10 sm:p-8"><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[.18em] text-emerald-700">Step 01</p><h2 className="mt-2 text-2xl font-semibold tracking-tight text-slate-950">Start with your project area</h2></div><div className="flex h-11 w-11 items-center justify-center rounded-2xl bg-emerald-50 text-emerald-700"><Ruler className="h-5 w-5" /></div></div><p className="mt-3 text-sm leading-6 text-slate-500">First we measure the space. Then the guided questions help us understand what the material needs to do.</p><div className="mt-7 grid gap-4 sm:grid-cols-2"><label className="text-sm font-medium text-slate-700">Length (m)<input type="number" min="0" value={length} onChange={(event) => setLength(event.target.value)} placeholder="e.g. 10" className="mt-2 w-full rounded-xl border border-slate-200 bg-stone-50 px-4 py-3 outline-none transition focus:border-emerald-500 focus:bg-white focus:ring-4 focus:ring-emerald-500/10" /></label><label className="text-sm font-medium text-slate-700">Width (m)<input type="number" min="0" value={width} onChange={(event) => setWidth(event.target.value)} placeholder="e.g. 5" className="mt-2 w-full rounded-xl border border-slate-200 bg-stone-50 px-4 py-3 outline-none transition focus:border-emerald-500 focus:bg-white focus:ring-4 focus:ring-emerald-500/10" /></label></div><div className="relative my-5 flex items-center gap-3 text-xs font-semibold uppercase tracking-[.18em] text-slate-400"><div className="h-px flex-1 bg-slate-200" />or<div className="h-px flex-1 bg-slate-200" /></div><label className="text-sm font-medium text-slate-700">Total area (m²)<input type="number" min="0" value={area} onChange={(event) => setArea(event.target.value)} placeholder="Enter square meters directly" className="mt-2 w-full rounded-xl border border-slate-200 bg-stone-50 px-4 py-3 outline-none transition focus:border-emerald-500 focus:bg-white focus:ring-4 focus:ring-emerald-500/10" /></label><div className="mt-5 rounded-2xl bg-slate-950 p-5 text-white"><div className="flex items-center justify-between"><span className="text-sm text-slate-300">Calculated area</span><strong className="text-2xl text-emerald-300">{computedArea ? `${computedArea.toFixed(2)} m²` : "—"}</strong></div><p className="mt-2 text-xs text-slate-400">The estimator uses this area for quantity and per-m² service calculations.</p></div><div className="mt-6 grid gap-4 sm:grid-cols-2"><label className="text-sm font-medium text-slate-700">Delivery location<input value={deliveryCity} onChange={(event) => setDeliveryCity(event.target.value)} placeholder="City or municipality" className="mt-2 w-full rounded-xl border border-slate-200 bg-stone-50 px-4 py-3 outline-none focus:border-emerald-500" /></label><label className="text-sm font-medium text-slate-700">Delivery zone<select value={deliveryZoneId} onChange={(event) => setDeliveryZoneId(event.target.value)} className="mt-2 w-full rounded-xl border border-slate-200 bg-stone-50 px-4 py-3 outline-none focus:border-emerald-500"><option value="">Select a delivery zone</option>{deliveryZones.map((zone) => <option key={zone.id} value={zone.id}>{zone.name} · {formatPrice(zone.fee)}</option>)}</select></label></div><label className="mt-4 block text-sm font-medium text-slate-700">Preferred schedule<input value={timeline} onChange={(event) => setTimeline(event.target.value)} placeholder="e.g. Within 2 weeks" className="mt-2 w-full rounded-xl border border-slate-200 bg-stone-50 px-4 py-3 outline-none focus:border-emerald-500" /></label><label className="mt-4 block text-sm font-medium text-slate-700">Additional requirements<textarea value={notes} onChange={(event) => setNotes(event.target.value)} rows={3} placeholder="Access details, measurements, or other requirements" className="mt-2 w-full rounded-xl border border-slate-200 bg-stone-50 px-4 py-3 outline-none focus:border-emerald-500" /></label><Button size="lg" className="mt-6 w-full bg-emerald-600 hover:bg-emerald-700" onClick={startQuestionnaire} disabled={loading}>{loading ? "Preparing questions…" : "Calculate requirements"}<ArrowRight className="ml-2 h-5 w-5" /></Button>{error && <div className="mt-4 flex items-start gap-3 rounded-xl border border-red-200 bg-red-50 p-4 text-sm text-red-700"><CircleHelp className="mt-0.5 h-4 w-4 shrink-0" />{error}</div>}{message && <div className="mt-4 flex items-start gap-3 rounded-xl border border-emerald-200 bg-emerald-50 p-4 text-sm text-emerald-800"><CheckCircle2 className="mt-0.5 h-4 w-4 shrink-0" />{message}</div>}</div>

      <div className="space-y-6">{questionnaireOpen ? <section className="rounded-[2rem] border border-emerald-200 bg-white p-6 shadow-xl shadow-emerald-900/10 sm:p-8"><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[.18em] text-emerald-700">Project question {questionIndex + 1} of {QUESTIONS.length}</p><h2 className="mt-2 text-2xl font-semibold tracking-tight text-slate-950">{question.title}</h2></div><div className="rounded-2xl bg-emerald-50 px-3 py-2 text-xs font-semibold text-emerald-700">{Math.round(((questionIndex + 1) / QUESTIONS.length) * 100)}%</div></div><p className="mt-3 text-sm leading-6 text-slate-500">{question.help}</p><div className="mt-6 grid gap-3 sm:grid-cols-2">{question.options.map((option) => { const selected = profile[question.key] === option.value; return <button key={option.value} onClick={() => chooseAnswer(option.value)} className={`rounded-2xl border-2 p-4 text-left transition hover:-translate-y-0.5 ${selected ? "border-emerald-500 bg-emerald-50" : "border-slate-200 hover:border-emerald-200"}`}><div className="flex items-start justify-between gap-3"><div><h3 className="font-semibold text-slate-950">{option.label}</h3><p className="mt-1 text-sm leading-6 text-slate-500">{option.description}</p></div>{selected && <span className="flex h-7 w-7 shrink-0 items-center justify-center rounded-full bg-emerald-600 text-white"><Check className="h-4 w-4" /></span>}</div></button>; })}</div><div className="mt-7 flex items-center justify-between gap-3"><button onClick={() => questionIndex > 0 ? setQuestionIndex((current) => current - 1) : setQuestionnaireOpen(false)} className="text-sm font-semibold text-slate-500 hover:text-slate-950">{questionIndex > 0 ? "Back" : "Change area"}</button><Button onClick={() => void nextQuestion} className="bg-emerald-600 hover:bg-emerald-700">{questionIndex === QUESTIONS.length - 1 ? "Show matching materials" : "Continue"}<ArrowRight className="ml-2 h-4 w-4" /></Button></div></section> : <><section className="rounded-[2rem] border border-slate-200 bg-white p-6 shadow-lg shadow-slate-900/5 sm:p-8"><div className="flex items-start justify-between"><div><p className="text-xs font-semibold uppercase tracking-[.18em] text-emerald-700">Step 02</p><h2 className="mt-2 text-2xl font-semibold tracking-tight text-slate-950">Materials matched to your project</h2></div><PackageCheck className="h-6 w-6 text-emerald-700" /></div><p className="mt-2 text-sm leading-6 text-slate-500">We rank active, in-stock products using your answers, then calculate the quantity from each product&apos;s configured coverage.</p>{!recommendations.length ? <div className="mt-6 rounded-2xl border border-dashed border-slate-300 bg-stone-50 p-8 text-center"><PackageCheck className="mx-auto h-9 w-9 text-slate-400" /><p className="mt-3 font-medium text-slate-700">Your shortlist will appear here</p><p className="mt-2 text-sm leading-6 text-slate-500">Click Calculate Requirements, answer the project questions, and we will compare the available inventory.</p></div> : <div className="mt-6 grid gap-4">{recommendations.map((item, index) => { const selected = selectedRecommendation?.id === item.id; return <button key={item.id} onClick={() => void chooseMaterial(item.id)} className={`w-full rounded-2xl border-2 p-4 text-left transition hover:-translate-y-0.5 hover:shadow-lg ${selected ? "border-emerald-500 bg-emerald-50/70" : "border-slate-200 hover:border-emerald-200"}`}><div className="flex gap-4"><div className="flex h-24 w-24 shrink-0 items-center justify-center overflow-hidden rounded-xl bg-stone-100">{item.image ? <img src={item.image} alt={item.name} className="h-full w-full object-cover" /> : <span className="text-3xl text-slate-400">◈</span>}</div><div className="min-w-0 flex-1"><div className="flex items-start justify-between gap-3"><div><div className="flex flex-wrap items-center gap-2"><h3 className="font-semibold text-slate-950">{item.name}</h3>{index === 0 && <span className="rounded-full bg-emerald-100 px-2 py-1 text-[10px] font-bold uppercase tracking-[.12em] text-emerald-700">Best match</span>}</div><p className="mt-1 line-clamp-2 text-sm leading-5 text-slate-500">{item.description}</p></div>{selected && <span className="flex h-7 w-7 shrink-0 items-center justify-center rounded-full bg-emerald-600 text-white"><Check className="h-4 w-4" /></span>}</div><div className="mt-3 flex flex-wrap gap-2">{(item.matchReasons || []).map((reason) => <span key={reason} className="rounded-full bg-slate-100 px-2.5 py-1 text-xs text-slate-600">{reason}</span>)}</div></div></div><div className="mt-4 flex items-end justify-between gap-3 border-t border-slate-200/80 pt-3"><div><p className="text-xs text-slate-500">Estimated requirement</p><p className="text-xl font-bold text-slate-950">{item.estimate.recommendedQuantity} {item.unit}</p></div><div className="text-right"><p className="text-xs text-slate-500">Material estimate</p><p className="font-semibold text-emerald-800">{formatPrice(item.estimate.materialTotal)}</p></div></div></button>; })}</div>}<button onClick={() => { setQuestionnaireOpen(true); setQuestionIndex(0); }} className="mt-5 text-sm font-semibold text-emerald-700 hover:text-emerald-800">Review or change project answers</button></section><section className="rounded-[2rem] border border-slate-200 bg-white p-6 shadow-lg shadow-slate-900/5 sm:p-8"><div className="flex items-start justify-between"><div><p className="text-xs font-semibold uppercase tracking-[.18em] text-emerald-700">Step 03</p><h2 className="mt-2 text-2xl font-semibold tracking-tight text-slate-950">Add delivery or installation</h2></div><Wrench className="h-6 w-6 text-emerald-700" /></div><p className="mt-2 text-sm leading-6 text-slate-500">Select service cards to include them in your live estimate.</p>{!services.length ? <div className="mt-5 rounded-2xl border border-dashed border-slate-300 bg-stone-50 p-6 text-center text-sm text-slate-500">No services are configured yet. You can still request a material-only quotation.</div> : <div className="mt-5 grid gap-3 sm:grid-cols-2">{services.map((service) => { const active = selectedServiceIds.includes(service.id); return <button key={service.id} onClick={() => void toggleService(service.id)} className={`rounded-2xl border-2 p-4 text-left transition ${active ? "border-emerald-500 bg-emerald-50" : "border-slate-200 hover:border-emerald-200"}`}><div className="flex items-start justify-between"><div className={`flex h-10 w-10 items-center justify-center rounded-xl ${active ? "bg-emerald-600 text-white" : "bg-stone-100 text-slate-500"}`}>{service.name.toLowerCase().includes("delivery") ? <Truck className="h-5 w-5" /> : <Wrench className="h-5 w-5" />}</div>{active && <CheckCircle2 className="h-5 w-5 text-emerald-700" />}</div><h3 className="mt-4 font-semibold text-slate-950">{service.name}</h3><p className="mt-1 text-sm leading-6 text-slate-500">{service.description}</p><p className="mt-4 text-sm font-semibold text-emerald-800">{formatPrice(service.total || service.price)}{service.pricingModel === "per_sqm" ? " / m²" : ""}</p></button>; })}</div>}</section><section className="sticky top-24 rounded-[2rem] bg-slate-950 p-6 text-white shadow-2xl shadow-slate-900/20 sm:p-8"><div className="flex items-start justify-between"><div><p className="text-xs font-semibold uppercase tracking-[.18em] text-emerald-300">Step 04</p><h2 className="mt-2 text-2xl font-semibold">Your quotation summary</h2></div><CheckCircle2 className="h-6 w-6 text-emerald-300" /></div><div className="mt-7 space-y-4 text-sm"><div className="flex justify-between gap-4"><span className="text-slate-400">Project area</span><strong>{computedArea ? `${computedArea.toFixed(2)} m²` : "—"}</strong></div><div className="flex justify-between gap-4"><span className="text-slate-400">Recommended material</span><strong className="max-w-[55%] text-right">{selectedRecommendation?.name || "—"}</strong></div><div className="flex justify-between gap-4"><span className="text-slate-400">Estimated quantity</span><strong>{selectedRecommendation ? `${selectedRecommendation.estimate.recommendedQuantity} ${selectedRecommendation.unit}` : "—"}</strong></div><div className="border-t border-white/10 pt-4"><div className="flex justify-between py-1"><span className="text-slate-400">Materials</span><span>{formatPrice(currentTotals.materialTotal)}</span></div><div className="flex justify-between py-1"><span className="text-slate-400">Delivery</span><span>{formatPrice(currentTotals.deliveryFee)}</span></div><div className="flex justify-between py-1"><span className="text-slate-400">Services</span><span>{formatPrice(currentTotals.serviceTotal)}</span></div><div className="mt-4 flex items-end justify-between border-t border-white/10 pt-4"><span className="text-slate-300">Estimated total</span><strong className="text-3xl font-semibold text-emerald-300">{formatPrice(currentTotals.total)}</strong></div></div></div><p className="mt-6 text-xs leading-6 text-slate-400">{DISCLAIMER}</p><Button size="lg" className="mt-6 w-full bg-emerald-400 text-slate-950 hover:bg-emerald-300" onClick={requestQuotation} disabled={submitting}>{submitting ? "Submitting…" : isAuthenticated ? "Request this quotation" : "Sign in to request quotation"}<ArrowRight className="ml-2 h-5 w-5" /></Button><div className="mt-4 flex items-center gap-2 text-xs text-slate-500"><MapPin className="h-4 w-4" /> Recommendations only include active in-stock inventory.</div></section></>}</div></div></section></main>;
}
